opensearch-pipeline:
  source:
    opensearch:
      hosts : "http://localhost:9200/"
      index : "movies"
      datasource : "opensearch"
      keep_alive : "5m"
      query : ""
      scroll : "10m"
      size : "10000"
  sink:
    - stdout:

